when@prod:
    sentry:
        dsn: '%env(SENTRY_DSN)%'
        options:
            release: '%env(default::SENTRY_RELEASE)%'
            environment: 'production'
            before_send: App\Flags\Service\SentryBeforeSend
