lexik_jwt_authentication:
    # Public key is fetched from JWKS endpoint and cached to this file
    public_key: '%kernel.project_dir%/config/jwt/public.pem'
    user_id_claim: sub
    token_ttl: 3600
