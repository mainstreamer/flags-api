lexik_jwt_authentication:
    # We use a custom JWKS-based encoder, no local keys needed
    # The public key is fetched from the auth server's JWKS endpoint
    user_id_claim: sub
    token_ttl: 3600
